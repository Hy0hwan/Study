import { Button } from '@/components/ui/button';

const Bookmark = () => {
  return (
    <Button className="bg-gray-300 flex items-center font-jua text-xl ">
      <p>북마크</p>
    </Button>
  );
};

export default Bookmark;
